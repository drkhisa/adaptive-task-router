interface:
  display_name: "Adaptive Task Router"
  short_description: "Choose the right surface, model, and reasoning effort."
  default_prompt: "Use $adaptive-task-router to recommend the minimum sufficient configuration for my task."
policy:
  allow_implicit_invocation: true
